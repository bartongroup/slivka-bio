---
baseCommand:
- jpred

inputs:
  input:
    arg: $(value)
    type: file
    
outputs:
  archive:
    path: "*.tar.gz"
    media-type: application/gzip

runners:
  default:
    class: scripts.runners.JPredRunner
...
